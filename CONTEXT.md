# Fuyugyō ドメイン用語集

スキー・スノーボードスクールの運営管理（インストラクター管理、シフト作成・割り当て、LINE 認証、招待）を扱うシステムのユビキタス言語。実装詳細ではなく「概念が何であるか」を定義する。

## アカウントと人材

**User（アカウント）**:
LINE 認証されたログイン主体。権限ロールを持ち「誰がシステムを操作できるか」を表す。`Instructor` とは別概念で、任意に最大1リンクされる（同一人物の「ログイン上の顔」）。
_Avoid_: アカウント単体を指して「インストラクター」と呼ばない

**Instructor（インストラクター）**:
シフトに割り当て可能な人材。「誰がシフトに入るか」を表す。ログインの有無とは独立し、`User` を持たないインストラクターも存在する（システムを使わないがシフトには載る人）。逆にシフトに入らない `User`（管理者・閲覧者）も存在する。User ↔ Instructor は基本 1:1（リンク時）。

**Role（ロール）**:
User の権限段階。ADMIN > MANAGER > MEMBER の3段階の序列を持つ。
_Avoid_: 権限、パーミッション（個別の許可を指す場合を除く）

## 組織と資格

**Department（部門）**:
スクールの区分を表す、小さく安定した**固定分類語彙**。現在はスキー／スノーボードの2つで、増減は稀かつ意図的な決定であり、日々のデータ入力ではない（運用中に管理されるエンティティではなく、実質クローズドな語彙）。Certification と Shift はそれぞれ**ちょうど1つ**の Department に属する「最上位の分類」。一方 Instructor は単一の Department に分類されない——保有資格（Certification）を通じて複数部門にまたがりうるため、「その人の部門」は単一属性ではなく**集合**として捉える。
_Avoid_: 部署、課／Instructor を単一部門に固定して語らない

**Certification（資格）**:
インストラクターが保有しうる資格。いずれかの Department に属し、Instructor とは多対多。
_Avoid_: 認定、ライセンス

## シフト

**Shift（シフト）**:
特定の「日付 × 部門 × シフト種別」で一意に定まる**勤務枠**。複数のインストラクターを `ShiftAssignment` 経由で収容できる。1人の勤務予定そのものではなく「枠」である点に注意。
_Avoid_: 「あるインストラクターのシフト」の意味で単独使用しない（それは ShiftAssignment）

**ShiftType（シフト種別）**:
シフトの種類を表す**部門横断の共有カタログ**（例: 終日／午前／午後）。Shift がどの種別かを定める。単一の Department には属さず（Certification と異なる点）、どの部門で使えるかは `DepartmentShiftType` が別に表す。全部門で一括して隠す `is_active`（カタログ無効化）を持つ。
_Avoid_: シフトカテゴリ／種別を単一部門に固定して語らない

**DepartmentShiftType（部門別シフト種別）**:
「ある Department がある ShiftType を使えるか」と「その部門内での表示順」を表す、Department × ShiftType の可用性の結合。部門ごとに使う種別を絞り込み、優先度順に並べるための唯一の真実。ここに無い (部門, 種別) の組では新規 Shift を作成できない（既存 Shift の表示・保持には影響しない）。
_Avoid_: 「シフト種別そのもの」と混同しない（それは ShiftType）／部門コードは固定語彙（ADR 0011）

**ShiftAssignment（シフト割り当て）**:
1つの Shift に1人の Instructor を割り当てた関係。Shift と Instructor の多対多を表す結合。「インストラクター個人の勤務予定1件」はこれを指す。
_Avoid_: アサイン単体を「シフト」と呼ばない

## 稼働

**稼働日（Operating Day）**:
スクールが開講しシフトが組まれる日。1件以上の Shift が存在する日を指す。運用上は週末（土日祝）に増員し、平日は基本的に縮小営業（少人数）となる。平日稼働の有無・規模は部門ごとに異なる。
_Avoid_: 営業日（一般語との混同を避ける）

**休校日（Closed Day）**:
スクールが開講しない日。マスタでは管理せず、「1件も Shift が入っていない日」を暗黙的に休校日とみなす。

**負荷（Workload）**:
ある Instructor の一定期間における勤務の過密度。割り当て時の偏り・過重の判断に用いる。月内の延べ勤務日数・シーズン累計勤務日数・連続週末数・連続勤務日数で測る。
_Avoid_: 稼働率（枠の充足率と混同しない）

## 招待

**Invitation / InvitationToken（招待）**:
新規 User がサインアップするための URL トークン。有効期限・最大使用回数・使用済み回数を持ち、ある User によって発行される。
_Avoid_: 招待コード（URL トークン方式である点を曖昧にしない）
